# 本地修改记录

记录本仓库中**尚未提交**的本地改动，便于对照与回滚。合并或提交后可删除本文件。

基线：`391c72f`（Merge pull request #127 from Nigh/v4）
日期：2026-09-13

- 已完成改动：9 个已跟踪文件（+75 / −23）+ 3 个新增文件
- 计划中改动：见第三部分，**尚未实施**

---

## 一、新增 Legado（开源阅读）书源

让未改动的 Legado 客户端通过网络导入书源，直接阅读本项目的全部小说。

**书源地址：`http://<地址>:48090/api/legado/book-source.json`**

```text
在 Legado 中打开「书源管理 → 网络导入」，粘贴上面的地址。
<地址> 换成实际访问本程序的地址（局域网 IP、域名或隧道均可）。
导入后搜索即可看到全部项目；书籍与章节地址按请求地址自动推导，无需额外配置。
```

| 文件 | 行数 | 说明 |
|---|---|---|
| `internal/httpapi/legado.go` | 新增 368 | 4 个只读端点；`baseURL` 按请求 Host（含 `X-Forwarded-Proto`）推导绝对地址；章节标题按项目语言补序号 |
| `internal/httpapi/legado_test.go` | 新增 44 | `chapterDisplayTitle` / `hasChapterPrefix` 表驱动测试 |
| `internal/httpapi/web.go` | +7 | 注册 `GET /api/legado/book-source.json`、`/search`、`/book/{project}`、`/chapter/{project}/{num}` |
| `AGENTS.md` | +6 | 架构边界补一句，新增「Legado 书源」小节 |

前端未改动；README 未改动（书源地址只记录在本文件）。

相对参考提交 `948e85e` 的必要适配：`StorySynopsis` → `LongTermDirection`；书名与目录只读 `progress.json` 元数据、仅正文走 `story.LoadProgress`；`{project}` 改用 `validProjectName` 校验以阻断路径穿越；语言判断改用 `i18n` 常量。

## 二、手机浏览器显示修复

窄屏（`md` 以下，48rem）改为单列：导航变顶部横向滚动条，助理改为右下角按钮打开的全屏浮层；桌面三栏布局不变。

| 文件 | 行数 | 说明 |
|---|---|---|
| `frontend/src/App.svelte` | +27 / −10 | 窄屏单列布局、助理浮层、右下角按钮、头部间距 |
| `frontend/src/app.css` | +17 | `100dvh`（保留 `100vh` 回退）、窄屏输入框 ≥16px 防 iOS 缩放、隐藏导航滚动条 |
| `frontend/src/pages/Config.svelte` | +8 / −8 | 6 处 `grid-cols-2` → `grid-cols-1 sm:grid-cols-2`，2 处 `col-span-2` → `sm:col-span-2` |
| `frontend/src/pages/Writing.svelte` | +3 / −3 | 正文网格改容器查询 `@2xl:`，进度卡片按钮行换行 |
| `frontend/src/pages/Proofread.svelte` | +1 / −1 | 正文网格改容器查询 `@2xl:` |
| `frontend/src/lib/i18n/zh.js` | +1 | 新增 `chat.panel.title` |
| `frontend/src/lib/i18n/en.js` | +1 | 同上 |
| `AGENTS.md` | +4 | 前端约定补窄屏布局与 `100dvh` 两条约束 |

---

## 三、计划中的优化（尚未实施）

参考上游 `原本个人fork版show-me-the-story` 的对应提交。以下只是计划，本仓库当前未做任何改动。

### 3.1 聊天支持附带文件（图片视觉 + 文本注入）

参考提交 `1a9361c`（上游改动面：15 文件，+632 / −40）

**目标**：聊天可以附带图片和文本文件，图片走多模态视觉、文本内容注入提示词。

| 层 | 计划改动 |
|---|---|
| `internal/llm/api.go` | `Message.Content` 由 `string` 改为 `any`：纯文本仍序列化为字符串，附件消息序列化为多部分 content 数组；新增 `ContentPart` / `imageURL` / `ImageContentPart()` / `TextContentPart()`；响应解析拆出独立的 `respMessage`（避免 `Message` 变 any 后影响反序列化） |
| `internal/llm/tokens.go` | 新增 `ContentRuneLen(any) int`：字符串按 rune 数，多部分按文本 rune 数 + 每张图片 500 名义值估算 |
| `internal/story/chat_attachments.go`（新增） | 白名单 jpg/png/gif/webp + txt/md；单文件 ≤8MB、每次 ≤5 个、总量 ≤20MB；落盘 `sessions/{id}/attachments/`；文件名/会话 ID/相对路径三段穿越防护；文本注入截断 2 万字符 |
| `internal/story/chat.go` | `ChatMessage` 增加 `Attachments []ChatAttachment`；`DeleteChatSession` 一并清理附件目录 |
| `internal/agent/agent.go` | `RunAgentLoop` 增加 `attachments` 参数，**仅当前轮**用户消息组装多部分内容 |
| `internal/httpapi/` | `POST /api/chat/sessions/{id}/messages` 接收 base64 附件；新增 `GET /api/chat/sessions/{id}/attachments/{file}`；新增错误 key `attachment_invalid` / `attachment_not_found` 与消息 key `agent.attachment_failed` |
| `frontend/src/components/ChatPanel.svelte` | 📎 选择 / 预览 / 移除附件、发送时上传、用户气泡渲染缩略图、重试复用附件 |
| i18n | `chat.attach.*` 中英各 +5 |

**落地注意**

- 本地的 `llm.Message.Content` 目前仍是 `string`、`RunAgentLoop` 无 `attachments` 参数，这正是本项的核心改动；其余前置条件（`isValidSessionID`、`sessionsDir`、`fsutil`、`ChatPanel.svelte`）本地都已具备。
- 上游该提交基于 `project_compat.go` 仍存在的版本，与本仓库现状一致，冲突风险低。
- 只给当前轮用户消息带附件，历史消息不重复回传图片 —— 否则多轮对话 token 会迅速膨胀。
- 删除会话、删除项目、备份/恢复等多处都会途经会话目录，新增附件目录后要与现有清理/备份路径对齐。

### 3.2 项目选择页支持重命名

参考提交 `70b914f`（上游改动面：10 文件，+199 / −36）

**目标**：项目列表可直接重命名项目，重命名当前项目时不丢状态。

| 层 | 计划改动 |
|---|---|
| `internal/httpapi/web.go` | 新增 `PUT /api/projects/{name}` |
| `internal/httpapi/handlers.go` | 抽出 `updateProjectPathsLocked(name)` 统一重算 `cfgPath` / `progressPath` / `settingsPath` / `sessionsDir` / `postprocessPath`，`switchProject` 一并复用；重命名流程：任务运行中拒绝 → 校验 → `os.Rename` 目录 → 改名当前项目时同步路径并返回 `was_current:true` |
| 校验 | 非法字符 / 重名（409）/ 同名（400）/ 不存在（404） |
| `frontend/src/pages/Projects.svelte` | hover 显示「重命名」，内联输入框 Enter 保存 / Esc 取消，成功后刷新列表并同步 `currentProject` |
| i18n | 后端错误 key `project_name_same` / `rename_project_failed`、消息 key `log.project_renamed`；前端 `projects.rename` / `projects.rename.placeholder` / `projects.toast.renamed` |

**落地注意**

- 上游为此新增了一个 `validateProjectName`，比本地的 `validProjectName` 宽松。本地应**直接复用已有的 `validProjectName`**（已含 `.restore-` 前缀、Windows 保留名、`filepath.IsLocal`、控制字符等校验），不要引入第二套校验逻辑。
- 本地 `Projects.svelte` 每行比上游多一个「备份 ZIP」按钮，重命名按钮要并入同一组 hover 操作，注意别把备份挤掉，窄屏下也要能点到（参考第二部分）。
- 重命名后必须同步 `currentProject`，否则 `$currentProject === p.name` 的当前项目高亮和后续请求都会失配。
- 项目名同时是目录名；重命名只动目录，不触碰项目内部任何文件内容。
- `progress.json.rollback` 等事务文件路径由项目目录推导，重命名需要确认没有正在进行中的保存事务（任务互斥已覆盖大部分情况，但值得在实现时确认）。

---

## 生效方式

前端产物通过 `embed.FS` 嵌入二进制，改动需重新编译：

```bash
task build      # 前端 + Go 全量构建
task build:go   # 只构建 Go（要求 frontend/dist 已存在）
```

## 已执行的检查

```bash
go build ./... && go vet ./... && go test ./...
go test -race ./internal/httpapi/
cd frontend && npm run build && npm run check
```
